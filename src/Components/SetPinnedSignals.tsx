import React from 'react';

const SetPinnedSignals = () => {

    //should be a component and included in manage signals page?
    return (
        <div>

        </div>
    );
};

export default SetPinnedSignals;